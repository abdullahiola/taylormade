# ══════════════════════════════════════════════════════
#   TaylorMade — Environment Variables Reference
# ══════════════════════════════════════════════════════

# ── BACKEND  (copy to: backend/.env on your VPS) ──────

TELEGRAM_BOT_TOKEN=8588082641:AAE2CMBDqPFLvsBWFhzc15UwU8yl3pQ7cR0
TELEGRAM_CHAT_ID=5669470898
SMTP_EMAIL=jackgriffo49@gmail.com
SMTP_PASSWORD=swozifcsvcsnscwu
JWT_SECRET=taylormade-super-secret-jwt-key-2024
FRONTEND_URL=http://187.77.183.130

# Docker sets this automatically — leave commented unless needed
# DATABASE_URL=sqlite:////data/taylormade.db


# ── FRONTEND  (add these in Vercel → Project Settings → Environment Variables) ─

# Routes frontend calls through the Next.js /api/proxy serverless relay
# (avoids HTTPS→HTTP mixed content block)
NEXT_PUBLIC_API_URL=/api/proxy

# Server-side only — tells the proxy where to forward requests
# NOT exposed to the browser
BACKEND_URL=http://187.77.183.130


# ── LOCAL DEV  (copy to: .env.local for local development) ───────────────────

# NEXT_PUBLIC_API_URL=http://localhost:8000
